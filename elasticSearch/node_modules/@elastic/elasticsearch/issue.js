'use strict'

const { Client } = require('./index')

const client = new Client({
  nodes: [
    'https://elastic:changeme@localhost:9200',
    'https://elastic:changeme@localhost:9201',
    'https://elastic:changeme@localhost:9202'
  ],
  ssl: {
    rejectUnauthorized: true
  }
})

client.search({}).then(console.log).catch(console.log)
